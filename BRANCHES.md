# 分支功能设定

本仓库保留两个长期功能分支：`feature/online-mvp` 和 `feature/lan-play`。两个分支共享同一套游戏规则、消息协议和服务端状态机；差异只应体现在运行环境和联机入口。

## feature/online-mvp

标准在线对战 MVP 分支，面向后续公网部署或服务器运行。

- 前端：React + Vite 客户端，默认开发地址为 `http://localhost:5173`。
- 服务端：Node.js + `ws` WebSocket 服务，默认端口为 `8787`。
- 连接方式：开发环境自动连接当前页面 hostname 的 `8787`；生产环境可通过 `VITE_WS_URL` 指定 WebSocket 地址。
- 使用场景：部署到公网服务器、接入反向代理、HTTPS/WSS、进程守护和静态资源服务。
- 启动命令：`npm run dev`。

## feature/lan-play

局域网本地联机分支，面向同一 Wi-Fi 或同一局域网内的设备试玩。

- 继承 `feature/online-mvp` 的全部玩法能力和 UI 流程。
- 服务端默认监听 `0.0.0.0`，并在启动时打印可访问的局域网 WebSocket 地址。
- Vite 开发服务使用 `--host 0.0.0.0`，允许手机或另一台电脑访问主机的 Network 地址。
- 新增语义命令 `npm run dev:lan`，用于本地局域网联机启动。
- 使用场景：两台设备在同一局域网内，通过 `http://主机局域网IP:5173` 进入同一房间。

## 共同玩法规则

- 使用标准 54 张牌：52 张普通牌 + 小王 + 大王。
- 每局开始前先洗牌，随机移除 `8-20` 张暗牌，再洗剩余牌。
- 开局双方各摸 5 张；补牌时双方按当前回合顺序补到 5 张或牌库见底。
- 支持单张、对子、三张、三张及以上顺子、四张炸弹。
- 点数顺序为 `7 > 王 > 5 > 2 > 3 > K > Q > J > 10 > 9 > 8 > 6 > 4 > A`。
- 集齐 `7 + 任意王 + 5 + 2 + 3` 后可手动宣告胜利；牌库见底后先出完手牌者胜利。

## 同步原则

- 规则、消息类型、状态机、测试和核心 UI 修复应同步到两个分支。
- 局域网访问能力、LAN 启动脚本和局域网地址输出只属于 `feature/lan-play`。
- 公网部署配置、生产 WebSocket 地址和上线说明优先放在 `feature/online-mvp`。
- 如果两个分支都需要同一体验修复，先在目标分支完成验证，再 cherry-pick 或合并到另一个分支。
